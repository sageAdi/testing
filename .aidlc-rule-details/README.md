# Legacy AI-DLC Rule Details

This directory is retained as a legacy compatibility bundle from the original rules-heavy AI-DLC workflow.

The active source of truth for this repository is now:

- `AGENTS.md` for the lean always-on control plane
- `.agents/skills/**/SKILL.md` for on-demand stage procedures
- `.agents/references/**/*.md` for detailed contracts
- `.agents/generated-rules/AGENTS.md` for rule-only tool compatibility

Do not edit files in this directory for new AI-DLC behavior unless intentionally updating the legacy compatibility bundle.
